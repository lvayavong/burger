<li>
    {{name}}

    <button class="change-state" data-id="{{id}}" data-newstate="{{devoured}}">
        {{#if devoured}}Devoured!{{else}}Not!{{/if}}
    </button>

    <button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>